{
  "name": "kadicon-be-bun",
  "version": "0.0.1",
  "module": "src/index.js",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
